<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.rtmillerprojects.schedulerandnotifications.MainActivity">


    <Button
        android:id="@+id/jobservice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="LAUNCH JOB"
        android:onClick="jobMethod"
        />
    <Button
        android:layout_below="@+id/jobservice"
        android:id="@+id/alarmservice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="LAUNCH ALARM"
        android:onClick="alarmMethod"
        />
    <Button
        android:id="@+id/killalarm"
        android:layout_below="@+id/alarmservice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="KILL ALARM"
        android:onClick="killAlarm"
        />
    <Switch
        android:id="@+id/myswitch"
        android:layout_below="@+id/killalarm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
